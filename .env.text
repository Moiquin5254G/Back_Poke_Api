# Nombre de usuario para la base de datos
DB_USER

# Contraseña para la base de datos
DB_PASSWORD

# Host donde se encuentra la base de datos (por ejemplo, localhost o una IP)
DB_HOST

# Nombre de la base de datos que se utilizará
DB_NAME

# Puerto en el que la base de datos está escuchando
DB_PORT

# Puerto en el que el servidor escuchará las solicitudes
PORT
